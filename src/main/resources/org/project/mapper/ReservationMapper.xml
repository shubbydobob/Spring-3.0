<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.project.mapper.ReservationMapper">
 <insert id="insertReservation" parameterType="org.project.dto.ReservationDTO">
        INSERT INTO reservation (name, phone, address_postcode, address_road, address_bname, address_detail, problem, date, time)
        VALUES (#{name}, #{phone}, #{address_postcode}, #{address_road}, #{address_bname}, #{address_detail}, #{problem}, #{date}, #{time})
    </insert>
</mapper>